import 'package:flutter/material.dart';

const kPrimaryColor = Color.fromRGBO(15, 138, 190, 1);
const kSecondaryColor = Colors.white;
const kBgColor = Color.fromRGBO(246, 247, 248, 1);
const kGColor = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,

  colors: [Color(0xFF0F8ABE), Color(0xFF05203C)],
);

var kWColor = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  colors: [Colors.white.withOpacity(0.95), Colors.white.withOpacity(0.70)],
);

const kGreyColor = Color.fromRGBO(115, 115, 115, 1);

const kThirdColor = Colors.black;

const kErrorColor = Colors.red;
